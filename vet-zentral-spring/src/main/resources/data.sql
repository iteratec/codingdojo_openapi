CREATE TABLE animal (
                         id   INTEGER      NOT NULL AUTO_INCREMENT,
                         name VARCHAR(128) NOT NULL,
                         PRIMARY KEY (id)
);

INSERT INTO animal (id, name) VALUES (1, 'Unicorn');
